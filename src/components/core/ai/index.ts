/**
 * AI & Data Science visual vocabulary.
 *
 * Domain motifs shared across sections so the site reads as an AI/DS engineer's
 * portfolio rather than a generic premium template. All light-theme, blue-only,
 * decorative (hence `aria-hidden`) except `NotebookCell` and `Readout`, which
 * frame real content.
 */
export { default as NeuralField } from "./NeuralField";
export { LossCurve, EmbeddingScatter, ConfusionMatrix, TensorGrid } from "./charts";
export { NotebookCell, Readout, Brackets } from "./frames";
